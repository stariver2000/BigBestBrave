/**
 * 페이지가 통했는지 재는 기준.
 *
 * 이 사이트는 사람에 대해 아무것도 저장하지 않는다. 세는 것은 페이지마다 네 개의 수뿐이고,
 * 그 수만으로 답할 수 있는 물음만 묻는다 — 열어 보고 만졌는가, 만진 사람이 끝까지 갔는가.
 */

/** 세는 사건. 한 사람이 한 번 왔을 때 각각 많아야 한 번씩만 오른다. */
export const PULSE_KINDS = ['view', 'touch', 'reach', 'stay'] as const;

/**
 * 점수를 매기기 시작하는 최소 방문 수.
 *
 * 셋이 왔는데 둘이 만졌다고 67%라 부르면 그 수는 잡음을 따라 춤춘다.
 * 표본이 이보다 적으면 비율을 내지 않고 "아직 모른다"고 적는다.
 */
export const MIN_VIEWS = 20;

/** 머문 것으로 치는 시간(ms). 이보다 짧으면 열어 보고 닫은 것에 가깝다. */
export const STAY_AFTER_MS = 20_000;

/**
 * 판정 기준.
 *
 * 경계값은 이 페이지에서 정한 것이다. 논문이 제시한 문턱값이 아니며,
 * 실무에서 통용되는 감각(축을 자르면 부풀고, 45도에서 멀어지면 기울기를 잘못 읽는다)을 옮겼다.
 */

/** 이 배수를 넘으면 크게 어긋난 것으로 본다. */
export const SEVERITY = { high: 1.5, medium: 1.15 } as const;

/**
 * 선 그래프에서 기울기를 바르게 읽히게 하는 각도.
 * 평균 기울기가 45도(픽셀 좌표에서 1)에 가까울 때 사람이 변화율을 가장 정확히 읽는다는
 * 오래된 지침(banking to 45°)을 따랐다.
 */
export const IDEAL_SLOPE = 1;

/** 기울기가 이 배수 안쪽이면 문제 삼지 않는다. */
export const SLOPE_TOLERANCE = 1.4;

/** 계산이 무너지는 것을 막는 최소값. 0으로 나누는 자리를 지킨다. */
export const EPSILON = 1e-9;

# Development rules

이 저장소의 모든 코드는 아래 원칙을 따른다.

## 1. 가독성

1. 세 개 이상의 문법 요소가 한 표현에 중첩되면 중간 변수와 단계별 문장으로 풀어쓴다.
2. 리스트 컴프리헨션 안에서 여러 조건, walrus 연산자, 중첩 함수 호출을 함께 사용하지 않는다.
3. 짧은 코드보다 입력, 변환, 출력이 드러나는 코드를 우선한다.

## 2. 주석

1. 세 개 이상의 문법 요소를 함께 사용해야 한다면 실행 순서와 이유를 주석으로 설명한다.
2. 주석은 코드를 그대로 반복하지 않고 해당 로직이 그 위치에 있어야 하는 이유를 설명한다.
3. 문서 블록 경계, 표 병합, 좌표 변환처럼 데이터의 의미가 바뀌는 지점에는 반드시 설명을 남긴다.
4. 지금 구현에서는 쓰지 않지만 다른 기능을 위해 예약해 둔 필드/매개변수는  
 어떤 기능이 언제 쓰는지를 주석으로 명시한다. 명시할 수 없다면 넣지 않는다.

## 3. 확장성과 이식성

1. 파싱·변환 코어는 특정 웹 프레임워크, DB, 운영 애플리케이션에 의존하지 않는다.
2. 입력 파일 경로, 출력 경로, 외부 도구 경로 등 실행 설정은 외부에서 주입한다.
3. 모든 파서와 생성기는 공통 문서 모델(중간 표현)을 사용한다. 포맷별 자료구조가 코어 밖으로 새어 나가지 않는다.
4. CLI, MCP 서버 등 외부 인터페이스 연결은 코어와 분리된 별도 계층에 둔다.

## 4. 거시 구조 (파일·모듈 설계)

디렉토리 트리만 보고도 "이 코드는 어디에 있고, 새 코드는 어디에 넣어야 하는지"를  
처음 온 개발자가 알 수 있어야 한다.

1. 의존 방향은 한 방향으로 흐른다: `인터페이스(CLI 등) → 변환 조립 → 포맷 모듈 → 공통 문서 모델`.  
 공통 문서 모델은 어떤 포맷 모듈도 import하지 않는다.
2. 포맷별 파서/생성기는 각각 독립 모듈(디렉토리)로 두고, 포맷 모듈끼리는 서로 import하지 않는다.  
 공유가 필요한 로직은 공통 계층으로 내린다.
3. 새 포맷 추가는 기존 모듈의 수정 없이 새 디렉토리 추가와 등록만으로 가능해야 한다.
4. 파일 하나는 한 가지 책임만 진다. 한 파일이 화면 몇 장을 넘게 자라면(약 300줄)  
 책임이 섞였다는 신호로 보고 분리를 검토한다.
5. 모듈의 공개 API는 패키지 진입점 한 곳에서만 노출한다.  
 다른 모듈의 내부 파일을 직접 import하지 않는다.
6. 파일 읽기/쓰기 같은 I/O와 순수 변환 로직을 분리한다.  
 변환 로직은 바이트/객체를 받아 객체를 반환하는 순수 함수를 우선한다.
7. 테스트 디렉토리는 소스 구조를 그대로 미러링한다.  
 테스트 위치만 보고도 대상 코드를 찾을 수 있어야 한다.

## 5. 정리 원칙

코드를 수정하는 모든 변경은 정리를 포함한다. 기능 추가와 정리를 별도 작업으로 미루지 않는다.

1. 새 계약으로 전환한 뒤 이전 계약을 위한 호환 shim을 남기지 않는다.
2. 사용하지 않는 파일, 타입, import, 설정과 문서는 같은 변경에서 제거한다.
3. 코드를 수정할 때 그 주변에서 발견한 죽은 코드(호출되지 않는 함수, 도달하지 않는 분기)도 같은 변경에서 제거한다.
4. 목적을 알 수 없는 코드는 남겨두지 않는다. 조사해서 필요하면 이유를 주석으로 남기고, 불필요하면 제거한다.
5. 과거 구현은 소스 트리에 복사해 보존하지 않고 Git 이력에서 확인한다.
6. 설계 문서는 현재 구현과 일치하는 문서 하나를 기준으로 유지한다.

## 6. 동시 작업 규칙

이 저장소는 **여러 세션이 동시에 작업한다.** 페이지마다 다른 세션이 붙어 기존 페이지를 다듬고,
그와 별개로 새 페이지가 추가된다. 그래서 아래 규칙은 취향이 아니라 안전장치다.

### 6.1 소유권

페이지 하나는 아래 묶음을 소유한다. **남의 묶음을 고치지 않는다.**

```
src/core/<주제>/         그 페이지의 계산 코어
src/modules/<페이지>/     그 페이지의 설정·사전·화면
src/looks/<룩>/           그 페이지가 입는 룩
tests/core/<주제>/, tests/modules/<페이지>/
```

다른 페이지의 코드를 고쳐야 할 만큼 좋은 생각이 떠올랐다면, 고치지 말고 그 사실을 사람에게 알린다.

### 6.2 공유 파일

아래는 모두가 함께 쓰는 파일이다. **자기 줄만 더하고, 남의 줄은 건드리지 않는다.**
정렬·서식·이름 정리처럼 파일 전체를 훑는 변경은 하지 않는다. 그 한 번이 남의 작업을 통째로 덮는다.

| 파일 | 다루는 법 |
| --- | --- |
| `src/modules/index.tsx` | 자기 페이지 한 줄만 추가 |
| `src/looks/index.ts` | 자기 룩 한 줄만 추가 |
| `src/kit/ui/ui.module.css` | 바탕 스타일만. **룩 규칙을 여기 넣지 않는다** |
| `src/shell/shell.module.css` | 위와 같음 |
| `app/globals.css` | 되도록 손대지 않는다. 룩 변수로 해결한다 |
| `docs/ARCHITECTURE.md` | 자기 페이지 절만 추가 |

### 6.2-1 룩은 자기 CSS 파일을 갖는다

룩이 부품의 생김새를 바꿀 때는 공유 CSS에 규칙을 쌓지 않고 `src/looks/<룩>/look.css`에 쓴다.
그 파일은 그 룩만 소유하므로 다른 세션과 부딪히지 않는다.

겨누는 대상은 CSS 모듈의 클래스가 아니라 컴포넌트에 달린 `data-part` 이름표다.
클래스 이름은 빌드할 때마다 뒤섞이지만 이름표는 그대로다.

```css
/* src/looks/dossier/look.css */
[data-look='dossier'] [data-part='panel'] { ... }
[data-look='dossier'][data-part='frame']::before { ... }
```

이름표가 둘(`[data-look]` + `[data-part]`)이라 바탕 규칙(클래스 하나)보다 우선순위가 높다.
새 부품에 룩이 손댈 자리가 필요하면 그 컴포넌트에 `data-part`를 달고, 이름은 케밥 표기로 쓴다.

### 6.3 절대 깨진 채로 두지 않는다

가장 자주 사고가 나는 지점이다. 다른 세션이 그 순간 빌드를 돌리면 남의 작업이 멈춘다.

1. **참조보다 파일이 먼저다.** `import './Foo'`를 쓰기 전에 `Foo`를 만든다.
2. **사전 키가 먼저다.** 화면에서 `t('new-key')`를 쓰기 전에 세 언어 사전에 그 키를 넣는다.
3. **공용 코어를 옮기면 쓰는 곳을 같은 변경에서 함께 고친다.** 함수를 다른 모듈로 옮겨 놓고
 import를 남겨 두면 그 순간부터 저장소 전체가 컴파일되지 않는다.
4. 한 덩어리 작업을 마칠 때마다 `npx tsc --noEmit`과 `npx vitest run`을 돌려 초록인지 확인한다.

### 6.3-1 서버 띄우고 끄기

next 프로세스 둘이 같은 빌드 디렉토리를 물면 서로의 청크를 덮어 **모든 라우트가
500으로 죽는다**(2026-09-03에 실제로 났다: `Cannot find module './331.js'`).
tsc도 vitest도 초록인데 화면만 죽으므로 원인을 찾기 어렵다.

이건 규칙으로 막지 않는다. 규칙으로 두면 지키는 쪽이 아니라 **잊은 쪽이 남을
죽인다.** `next.config.mjs`가 기본값을 갈라 둔다.

- 개발 서버는 **포트마다** 다른 디렉토리를 쓴다(`.next-dev-3002`). 한 포트에 두
  서버가 못 뜨므로 개발 서버끼리 부딪히지 않는다.
- `.next`는 `next build`/`next start`만 쓴다. 그래서 오래 띄워 둔 운영 서버가
  누군가의 `npm run dev` 한 번에 죽는 일이 없다.

```bash
PORT=<빈 포트> npm run dev     # 이것으로 충분하다
```

정말 따로 두어야 할 때만 `NEXT_DIST_DIR`로 덮어쓴다. `.next-*`는 `.gitignore`에 있다.

끌 때는 **자기 PID만** 끈다. `pkill -f "next dev"`는 남의 서버까지 죽인다(같은 날
실제로 났다). 환경변수로 띄운 서버는 `--port`가 명령줄에 없으니 `pgrep`으로 못 찾는다.
포트로 찾고, 끄기 전에 그 프로세스의 환경을 보고 **내 것인지 확인한다.**

```bash
fuser -n tcp 3002                          # 그 포트를 쥔 PID
tr '\0' '\n' < /proc/<pid>/environ | grep -E '^(PORT|NEXT_DIST_DIR)='
```

**포트가 살아 있으면 그 빌드 디렉토리는 지우지 않는다.** 서버가 물고 있는 디렉토리를
지우면 그 서버는 그대로 500이 된다(2026-09-03에 실제로 났다). 디렉토리 이름이
`.next-dev-<포트>`이므로 검사는 기계적이다 — 지우기 전에 그 포트가 비었는지 본다.
서버를 먼저 내리고 지운다.

### 6.4 커밋

작업이 초록일 때마다 커밋한다. 커밋이 없으면 누가 무엇을 언제 바꿨는지 되짚을 수 없고,
덮어써진 작업을 되살릴 수도 없다. 커밋 메시지에 도구 이름이나 공동 저자 표시를 넣지 않는다.
